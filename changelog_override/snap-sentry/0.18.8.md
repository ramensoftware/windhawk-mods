* A notice now says when a multi-page or animated image is kept instead of deleted. Before it only showed in the log.
* More reliable folder watching on network and sync-backed locations that do not report changes.
* Large images are copied using less memory.
* Deleting to the Recycle Bin works again when the watched folder path ends in a separator.
